# 精读小结模板(全程跟随用户语言,默认中文;本地 Markdown)

读完一篇后按此格式输出,存到 `notes/作者年份_短标题.md`。
**主体字段领域无关**;末尾的"方向附加字段"从所选速查表的"小结附加字段"小节取,
没有就省略。

```
# [作者 年份] 标题

## 三句话总结
1.
2.
3.

## 核心创新点(1–3 条,每条对应到具体技术)
- 创新点 → 用到的关键方法/模块

## 方法(通用字段)
- 输入:
- 输出:
- 核心模块 / 架构:
- 训练目标 / 损失:

## 实验
- 数据(来源、规模、仿真/真实):
- 对比基线 + 评价指标:
- 主要结果(带数字):

## 方向附加字段
> 从所选 `references/domains/*.md` 的"小结附加字段"小节取;无则删掉本节。

## 对我课题的价值
- 可借鉴:
- 可对标 / 局限:
- 留下的空白:

## 我的疑问 / 待查
-
```

注:`核心创新点` 必须能指回 `方法` 里的具体技术;指不回去就是没读透,回去重读。
