# 示例 1:控制 + 深度学习论文陪读

**用户:** `$paper-reading-guide 带我读 papers/learned_mpc.pdf`

**技能:**
> (判定方向)这篇属于"控制 + 深度学习",我用 `control-dl.md` 当讲解基准。
>
> **60 秒速览卡**
> - 问题:传统 MPC 依赖准确动力学模型,真实系统难建模 → 控制性能掉。
> - 一句话做法:用神经网络从数据学动力学,嵌进 MPC 做预测。
> - punchline:[…宣称在 X 任务上比纯模型 MPC 跟踪误差降低若干…]
> - 建议路线:重点读 §3 方法、§4 实验;§2 背景可略。
>
> 走全程,还是只看方法?

**用户:** 全程,但慢点

**技能:**
> **§3.1 学习动力学模型**
> - 讲清楚:输入当前状态+动作,网络预测下一状态;用历史轨迹监督训练。
> - 拎重点:损失是多步预测误差(不是单步),目的是让长时域 MPC 预测不漂。
>   这是相对单步辨识的关键改动。
> - 连回去:动力学学好了,后面 §3.2 才能拿它在 MPC 里向前 rollout。
>
> 这节清楚了吗?要不要展开"多步损失为什么比单步好"?

**用户:** 继续

**技能:**
> **§4 实验** 里 Figure 3 是学习曲线(承载结论),我把它渲出来看:
> `python scripts/render_page.py papers/learned_mpc.pdf 6`
> → 已导出 figures/learned_mpc_p6.png。请打开看。这是回报随环境步数的曲线,
> 你重点看:本文方法(通常最上面那条)相对 baseline 收敛是不是更快、最终更高;
> 阴影带是多种子方差——方差大不大决定结论稳不稳。我不替你猜具体数值。
